# Arknights-Managiment-Sharing

## テスト

`frontend/` ディレクトリで実行する:

```
node --test
```

`test-dir/masterData.test.js` が `scripts/masterData.js` の純粋ロジック（列集合・セル解決・行生成）を
実マスターデータのフィクスチャ（`test-dir/fixtures/`）でビヘイビア固定する。
