# 列ソート手動確認

- 9本の静的列と、現在の `masterData.moduleIds.length` 本のモジュール列を順にクリックし、初回方向・逆方向・解除後の行順、マーカー、`aria-sort` が一致することを確認する。
- 言語切替・再描画後もモジュールヘッダ数が増えず、フィルタ適用中は active sort のまま絞り込み結果だけが並ぶことを確認する。
- OperatorName ソート中に ja → en → ch を切り替え、マーカーを保ったまま表示名で再ソートされることを確認する。
- sort 解除後は現在のフィルタ結果がマスター配列順に戻り、未操作時のセル値・列数・フィルタ件数表示が変わらないことを確認する。
